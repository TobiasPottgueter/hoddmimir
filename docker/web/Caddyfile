{
    admin off
    auto_https off
}

:8080 {
    root * /app/public
    encode zstd gzip

    @api path /api /api/*
    handle @api {
        php_server
    }

    handle {
        try_files {path} /index.html
        file_server
    }
}
